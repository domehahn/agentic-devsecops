---
title: "JSON-Reports: strukturierte Findings als stabile Automatisierungsschnittstelle"
description: "JSON ist das zentrale Maschinenformat für Scannerintegrationen. Es ermöglicht, Findings zuverlässig in Pipelines, Dashboards, Datenbanken und eigene Policy Engines zu übernehmen."
date: "2026-07-30"
author: "Dominik Hahn"
topics:
  - devsecops
  - agentic-engineering
tags:
  - Features
  - JSON
  - Security Scanning
featured: false
draft: false
series:
  slug: scanner-features
  title: "Scanner Features"
  order: 22
---

JSON ist das zentrale Maschinenformat für Scannerintegrationen. Es ermöglicht, Findings zuverlässig in Pipelines, Dashboards, Datenbanken und eigene Policy Engines zu übernehmen.

Das Feature ist damit nicht nur eine Komfortfunktion.

Es verändert, **welcher Kontext geprüft wird, welche Vertrauensgrenzen gelten und wie belastbar das Ergebnis später in CI/CD oder Security-Governance verwendet werden kann**.

## Was das Feature leistet

Ein gutes JSON-Schema trennt Metadaten, Artefaktidentität, Coverage, Findings und technische Fehler. Felder sollten stabile IDs und klar definierte Typen besitzen, damit Consumers nicht Freitext analysieren müssen.

Schema-Versionierung ist dabei entscheidend. Ein Reportformat ist eine API und sollte entsprechend kompatibel weiterentwickelt werden.

## Typischer Ablauf

Unabhängig von der konkreten Implementierung sollte der Ablauf klar getrennte Phasen besitzen:

```text
Input
  ↓
Validierung und Normalisierung
  ↓
Feature-spezifische Verarbeitung
  ↓
Security-Analyse
  ↓
Findings + Coverage + technischer Status
  ↓
Policy-Entscheidung
```

Diese Trennung ist wichtig, weil ein technischer Fehler bei Beschaffung, Parsing oder Providerzugriff nicht dieselbe Bedeutung hat wie ein tatsächlich unauffälliges Prüfergebnis.

## Negatives Beispiel

Ein Tool liefert:

```json
{
  "result": "Found HIGH issue in config.yaml line 18"
}
```

Alle Informationen stecken in einem String. Ein Downstream-System muss Text parsen und bricht, sobald sich die Formulierung ändert.

## Positives Beispiel

Ein strukturierter Befund sieht beispielsweise so aus:

```json
{
  "schema_version": "1.0",
  "findings": [
    {
      "id": "NET-004",
      "severity": "high",
      "path": "config.yaml",
      "line": 18,
      "message": "Unrestricted outbound endpoint"
    }
  ]
}
```

Dashboards können nach `severity` filtern, CI-Policies nach `id` entscheiden und Baselines auf stabile Fingerprints verweisen.

<KeyTakeaway>
JSON ist die Vertragsoberfläche des Scanners. Stabile IDs, Schema-Versionierung und klare Zustände sind wichtiger als eine möglichst kurze Ausgabe.
</KeyTakeaway>

## Security- und Betriebsaspekte

Auch strukturierte Reports können sensible Evidenz enthalten. Felder für Snippets, URLs oder Umgebungswerte benötigen Redaction-Regeln. Consumers sollten außerdem keine Reportdaten ungeprüft als HTML rendern.

Schemaänderungen sollten versioniert und getestet werden, damit Security Gates nicht durch unerwartete Nullwerte oder Feldumbenennungen fail-open reagieren.

Ein professioneller Betrieb sollte außerdem drei Zustände konsequent auseinanderhalten:

```text
clean      = geprüft, kein relevanter Befund
finding    = geprüft, mindestens ein relevanter Befund
incomplete = Prüfung technisch oder fachlich nicht vollständig
```

Gerade der dritte Zustand ist für DevSecOps entscheidend.

Ein Scanner darf fehlende Coverage nicht als positives Sicherheitsurteil darstellen.

## Wo das Feature sinnvoll eingesetzt wird

JSON eignet sich für APIs, CI, Data Lakes, eigene Dashboards und Integrationstests. Es ist das richtige Format, wenn ein Computer die Ergebnisse weiterverarbeiten soll.

Für menschliche Reviews ist Markdown oder Terminaldarstellung komfortabler.

Die konkrete Aktivierung sollte deshalb aus dem gewünschten Trust-Modell abgeleitet werden und nicht allein aus technischer Verfügbarkeit.

## Integration in eine DevSecOps-Pipeline

In einer Pipeline sollte das Feature nicht isoliert betrachtet werden.

Ein belastbares Muster ist:

```text
Source / Artifact
      ↓
Input-Validierung
      ↓
Deterministische Analyse
      ↓
optionale erweiterte Analyse
      ↓
normalisierte Findings
      ↓
Policy Gate
      ↓
Report / Audit Evidence
```

Dabei sollte jeder Report mindestens folgende Informationen transportieren:

- Identität des analysierten Artefakts
- aktive Analyseverfahren
- übersprungene oder nicht verfügbare Prüfschritte
- stabile Regel- oder Finding-IDs
- Schweregrad und Evidenz
- Scanner- und Regelversion
- eindeutiger technischer Abschlussstatus

Damit wird das Feature nicht nur ausführbar, sondern **auditierbar**.

## Fazit

JSON ist die Vertragsoberfläche des Scanners. Stabile IDs, Schema-Versionierung und klare Zustände sind wichtiger als eine möglichst kurze Ausgabe.

Der eigentliche Wert entsteht dann, wenn das Feature mit klaren Grenzen, reproduzierbaren Eingaben und transparenten Ergebnissen in den Entwicklungsprozess eingebettet wird.
